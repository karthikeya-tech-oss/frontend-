<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CertifyTrust</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #f9fbfd, #eef3f9);
      color: #333;
      text-align: center;
    }

    .header {
      padding: 30px;
    }

    .logo {
      font-size: 28px;
      font-weight: bold;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 8px;
    }

    .logo span {
      color: #007bff;
    }

    .features {
      display: flex;
      justify-content: center;
      gap: 30px;
      margin: 50px auto;
      flex-wrap: wrap;
    }

    .feature {
      background: white;
      padding: 25px;
      border-radius: 12px;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1);
      width: 280px;
      transition: transform 0.3s, box-shadow 0.3s;
      cursor: pointer;
    }

    .feature:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    }

    .icon {
      font-size: 40px;
      margin-bottom: 15px;
      color: #007bff;
    }

    .btn {
      display: inline-block;
      margin-top: 15px;
      padding: 12px 20px;
      border-radius: 6px;
      text-decoration: none;
      color: white;
      background: #007bff;
      font-weight: bold;
      transition: background 0.3s;
    }

    .btn:hover {
      background: #0056b3;
    }

    /* Modal */
    .modal {
      display: none;
      position: fixed;
      z-index: 1000;
      left: 0;
      top: 0;
      width: 100%;
      height: 100%;
      overflow: auto;
      background: rgba(0,0,0,0.5);
      justify-content: center;
      align-items: center;
    }

    .modal-content {
      background: #fff;
      padding: 30px;
      border-radius: 12px;
      width: 400px;
      max-width: 95%;
      text-align: left;
      box-shadow: 0 6px 18px rgba(0,0,0,0.2);
    }

    .modal-content h2 {
      margin-bottom: 20px;
    }

    .form-group {
      margin-bottom: 15px;
    }

    .form-group label {
      display: block;
      font-weight: bold;
      margin-bottom: 6px;
    }

    .form-group input[type="text"] {
      width: 100%;
      padding: 10px;
      border-radius: 6px;
      border: 1px solid #ccc;
    }

    .upload-box {
      border: 2px dashed #bbb;
      border-radius: 8px;
      text-align: center;
      padding: 20px;
      cursor: pointer;
      color: #666;
    }

    .submit-btn {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 6px;
      background: #007bff;
      color: white;
      font-weight: bold;
      cursor: pointer;
    }

    .submit-btn:hover {
      background: #0056b3;
    }

    .close {
      float: right;
      font-size: 22px;
      font-weight: bold;
      cursor: pointer;
      color: #666;
    }

    .close:hover {
      color: #000;
    }

    /* Why Choose Section */
    .why-section {
      margin: 60px auto;
      max-width: 900px;
    }

    .why-title {
      font-size: 24px;
      font-weight: bold;
      margin-bottom: 40px;
    }

    .why-features {
      display: flex;
      justify-content: space-around;
      flex-wrap: wrap;
      gap: 30px;
    }

    .why-item {
      width: 250px;
      text-align: center;
    }

    .why-icon {
      font-size: 36px;
      color: #007bff;
      margin-bottom: 15px;
    }

    .why-item h4 {
      margin-bottom: 10px;
    }

    /* Footer */
    .footer {
      background: #f1f5f9;
      padding: 20px;
      margin-top: 50px;
      font-size: 14px;
      color: #555;
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="logo">🔒 <span>CertifyTrust</span></div>
    <p>Verify academic credentials securely and instantly using blockchain technology</p>
  </div>

  <div class="features">
    <div class="feature" id="uploadBtn">
      <div class="icon">⬆️</div>
      <h3>Upload Certificate</h3>
      <p>Register your academic credentials on the blockchain for secure, tamper-proof verification</p>
      <div class="btn">Get Started</div>
    </div>

    <div class="feature" id="verifyBtn">
      <div class="icon" style="color:green;">🔍</div>
      <h3>Verify Certificate</h3>
      <p>Check the authenticity of academic credentials using our blockchain verification system</p>
      <div class="btn" style="background: green;">Verify Now</div>
    </div>
  </div>

  <!-- Upload Modal -->
  <div class="modal" id="uploadModal">
    <div class="modal-content">
      <span class="close" id="closeModal">&times;</span>
      <h2>Upload Certificate</h2>
      <div class="form-group">
        <label>Student Name</label>
        <input type="text" placeholder="Enter student's full name">
      </div>
      <div class="form-group">
        <label>Institution Name</label>
        <input type="text" placeholder="Enter institution name">
      </div>
      <div class="form-group">
        <label>Certificate File</label>
        <div class="upload-box">
          Click to upload or drag and drop <br> <small>PDF, JPG, PNG (max 10MB)</small>
        </div>
      </div>
      <button class="submit-btn">Upload & Register on Blockchain</button>
    </div>
  </div>

  <!-- Why Choose Section -->
  <div class="why-section">
    <div class="why-title">Why Choose CertifyTrust?</div>
    <div class="why-features">
      <div class="why-item">
        <div class="why-icon">🛡️</div>
        <h4>Secure & Tamper-Proof</h4>
        <p>Blockchain technology ensures certificates cannot be forged or altered</p>
      </div>
      <div class="why-item">
        <div class="why-icon" style="color:green;">⚡</div>
        <h4>Instant Verification</h4>
        <p>Verify credentials in seconds with our advanced blockchain lookup</p>
      </div>
      <div class="why-item">
        <div class="why-icon">🌍</div>
        <h4>Global Access</h4>
        <p>Access and verify certificates from anywhere in the world</p>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <div class="footer">
    Powered by Blockchain Technology
  </div>

  <script>
    // Open modal
    document.getElementById("uploadBtn").onclick = function() {
      document.getElementById("uploadModal").style.display = "flex";
    }

    // Close modal
    document.getElementById("closeModal").onclick = function() {
      document.getElementById("uploadModal").style.display = "none";
    }

    // Close if clicked outside modal
    window.onclick = function(event) {
      if (event.target === document.getElementById("uploadModal")) {
        document.getElementById("uploadModal").style.display = "none";
        document.querySelector(".submit-btn").onclick = async function() {
  const studentName = document.querySelector("input[placeholder=\"Enter student's full name\"]").value;
  const institutionName = document.querySelector("input[placeholder='Enter institution name']").value;
  const fileInput = document.createElement("input");
  fileInput.type = "file";
  fileInput.accept = ".pdf,.jpg,.jpeg,.png";
  
  fileInput.onchange = async () => {
    const file = fileInput.files[0];
    const formData = new FormData();
    formData.append("studentName", studentName);
    formData.append("institutionName", institutionName);
    formData.append("certificate", file);

    const res = await fetch("http://localhost:5000/upload", {
      method: "POST",
      body: formData
    });

    const result = await res.json();
    alert("✅ Registered on Blockchain!\nTxHash: " + result.txHash + "\nFileHash: " + result.fileHash);
  };

  fileInput.click();
};

      }
    }
  </script>
</body>
</html>
